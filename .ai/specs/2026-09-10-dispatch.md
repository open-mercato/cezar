# Task dispatch — a task may run other tasks

> Slug: `dispatch` · Status: implemented on `cez/c57e477b` · Default on; `CEZ_DISPATCH=0` turns it off → `capabilities.dispatch`
> Supersedes: `2026-09-08-units-hierarchy.md`, `2026-09-09-units-v2.md`, `2026-09-09-units-improvements-plan.md`
> (the missions/units feature was removed after the research in `units-research/00-verdict.md`).

## TLDR

There are no ranks and no missions. Any running task may dispatch other cezar tasks through a CLI,
`cez task create`, and a dispatched task reports back with `cez task report`. Each child runs in
its own worktree forked off the parent's committed branch, autonomously, with a budget carved out
of the parent's that returns unspent when the child settles. The child's report is delivered into
the parent's session; the parent validates, merges into its own branch, and never merges into the
base branch. A child dispatched with `--kind review` judges another task's branch and answers with
a verdict. Tasks communicate through a tree directory on disk; the engine is the signal.

The cockpit shows children nested under their parent in the task lists and the sidebar. One
default prompt template, "Review open PRs", is the worked example of a dispatching task. Nothing
else was added.

## Why this shape

The research (`units-research/`) found: hierarchy helps only on genuinely independent, parallel
work and hurts on coupled work; cheap workers pay only when verification is not cheap; a reviewer
reduces bias but cannot verify a completion claim; and unattended runs beyond ~1–2 hours of human
work mostly fail. So the primitive is a flat, per-task decision to dispatch, guarded by a prompt
that says when not to, with hard caps in the engine, and the review kind as the one validated
second-agent pattern.

## Resolved assumptions

| # | Question | Applied default | Why |
|---|---|---|---|
| A1 | How a task dispatches | The `cez task` CLI over the dispatch routes, with `CEZ_API_URL`, `CEZ_PROJECT_ID`, `CEZ_TASK_ID` in the agent's env. No turn-end markers. | Markers were the cause of every fatal refusal in the first missions; a CLI call gets a synchronous, typed answer. |
| A2 | Gating | ON by default; `CEZ_DISPATCH=0` turns it off — then both routes 409, the prompt is not composed and the composers hide the dispatching template. | Was opt-in (AGENTS.md § Zero config) until 2026-09-12: the owner flipped it after live run 3f7eaf02 — the brakes are in the engine, and a default-off dispatch made the agent fan out through its own sub-agents, unbudgeted and invisible. Recorded as an owner-approved exception in AGENTS.md. |
| A3 | Who becomes a root | Any task, on its first dispatch: `dispatch: { rootRunId: self }` is written then. | No composer, no size, no mission concept. |
| A4 | Fan-out and budget | 4 children in flight per task; a child's budget is carved out of the parent's and a settled child is charged its actual cost. | The brakes that worked in the missions; the release fix the audit demanded. |
| A5 | Parking | A turn that dispatched parks as a monitor (slot-exempt) when it ends; a child's settle wakes it through the delivery ladder. | Same mechanics as before, keyed on a per-turn flag set by `dispatch()`. |
| A6 | Files | `.ai/cezar/dispatch/<rootRunId>/`: `brief.md` (the root's task text), `units/<id8>/{order,notes,report}.md`, `inbox/<id8>/`, `ledger.jsonl`. Digest at session open; own inbox at turn end; wake of parked monitors at any task's turn end and settle. | The channel the missions proved; per-task-owned files, append-only inboxes. |
| A7 | Kinds | `implement` (default) and `review`. | Nothing else was asked for. |
| A8 | Dispatch off, but the agent was told to dispatch | The 409 and the CLI's no-URL exit both tell the agent to stop and report that dispatch is disabled; the "Review open PRs" template says the same and the composers hide it (`availablePromptTemplates`) until `capabilities.dispatch` is true. | Live run 3f7eaf02 (2026-09-11): the cockpit ran with dispatch off (then opt-in), the template was offered anyway, `cez task create` was refused, and the agent reviewed every PR itself with 54 Claude sub-agents — 4.3M tokens. |
| A9 | Autonomous nudge vs a repeated question | The first `CEZ:ASK` of an autonomous non-dispatch run is still overridden (#967); the SAME question asked again right after a nudge parks the run on it, with a note. | The same run: the agent asked "cez task create is refused … how should I proceed?" and the nudge overrode it 40/40 times. A verbatim repeat is a blocker, not a preference. |
| A10 | An in-place parent (worktree off) parks | It gives the exclusive working-tree lease back while parked — on a question or as a monitor — and takes it again before its session resumes (`parkRepoRoot` / `resumeRepoRoot`; wake-ups arriving meanwhile share one re-acquire). | Tester report 2026-09-11: an in-place task hung for an hour with free slots, waiting on the lease of a parent that was only watching its children. |
| A11 | Restart finds a `running` run with no agent session | It is re-queued whole (steps back to `pending`), not failed on "no agent session to resume". | Same report: after a restart the task was dead with nothing to resume. |
| A12 | Which runner and model a child gets | Omitted, it inherits the parent's; the prompt tells the parent to use what the user named, else to pick a cheaper or faster model for narrow, well-specified assignments. | Tester asked whether a specific agent can be chosen for children, and whether the model may choose itself. Both — through the existing `--runner` / `--model` flags, with guidance instead of a new switch. |
| A13 | Telling dispatched tasks apart in the lists | A child row carries a `review` / `implement` chip (the index entry now also carries `dispatch.kind`). | Tester asked for a label to identify dispatched tasks. |

## Contract (`packages/contract/src/dispatch.ts`)

- `runRecordSchema.dispatch?: { rootRunId, parentRunId?, kind?, reviewOf?, budgetUsd?, report?, pendingReports?, inboxSeenAt?, pendingAsk?, overBudget? }`.
- `POST /api/v1/p/:projectId/runs/:id/dispatch` body `dispatchInputSchema` (strict: title?, objective, kind?, review_of?, scope?, allowed_tools?, max_cost?, success_criteria?, required_evidence?, retry_limit?, runner?, model?) → `201 {id, branch?}` or `409 {error}`.
- `POST …/runs/:id/report` body `dispatchReportSchema` (status, result, evidence, confidence?, side_effects, errors, recommended_next_action?, verdict?, suggestions) → `{ok: true}` or `404`.
- `capabilities.dispatch` in `/health`.

## Engine (`packages/cezar/src/workflows/run.ts`, `src/dispatch/*`)

`dispatch()` (cap, budget carve, envelope, order/notes/brief/ledger, `dispatchedThisTurn`),
`recordReport()`, `handleDispatchTurn()` (budget brake, own-inbox delivery, inbox wake),
`reportSettledChildToParent()` (persist, report file, suggestions to the root's inbox, delivery
ladder), `cancelDescendants()`, `recordAsk()`/`clearPendingAsk()`; `tryAutonomousNudge` never fires
past a dispatch, a budget halt or a dispatched run's own question. `agentEnv` adds `CEZ_API_URL`,
`CEZ_PROJECT_ID` and `CEZ_TREE_DIR`.

## CLI (`packages/cezar/src/dispatch/task-cli.ts`)

`cez task create "<objective>" [--title] [--kind] [--review-of] [--scope] [--budget] [--success]
[--evidence] [--tools] [--runner] [--model] [--retry-limit]`, `cez task report --status --result
[--evidence …] [--verdict] [--suggestions …] [--confidence] [--side-effect …] [--error …] [--next]`,
`cez task list`, `cez task tree <run>`.

## Prompt (`packages/cezar/src/dispatch/prompts.ts`)

One `DISPATCH_PROMPT` in every task while the flag is on: when to dispatch (independent work) and
when not to (coupled work), the CLI, disjoint scopes, commit before dispatching, the 4-child cap,
monitoring park, reports as claims, merge into own branch only, review kind, the tree directory,
the Guard. `REVIEW_PROMPT` for `--kind review`: falsify a checklist, artefacts before narrative,
verdict required.

## Not done (deliberately)

Engine-executed verification commands, model escalation on failure, a live per-unit spend brake,
mission-scoped concurrency, missions routes and cockpit pages, ranks, ladders, per-repo role
prompt overrides. See `units-research/00-verdict.md` for the ranked follow-ups.
