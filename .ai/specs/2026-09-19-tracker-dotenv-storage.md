# Managed tracker dotenv storage

This document is a non-normative redirect, retained so existing links continue to resolve.

The single authoritative specification is [Browse Jira and Linear tasks alongside GitHub](2026-09-18-jira-linear-tracker-browsing.md). It defines project identity, credential storage and lifecycle, API/SSE contracts, protected surfaces, implementation phases and acceptance criteria.

The earlier text here is superseded in full. It adds no separate requirements and must not be used as evidence that a surface already exists on the base branch.
